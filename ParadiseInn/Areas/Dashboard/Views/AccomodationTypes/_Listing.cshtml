@model ParadiseInn.Areas.Dashboard.ViewModel.AllAccomodationTypesViewModel


@if (Model != null && Model.AccomodationTypes != null && Model.AccomodationTypes.Count() > 0)
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Description</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.AccomodationTypes)
            {
                <tr>
                    <th scope="row">@item.Name</th>
                    <td>@item.Description</td>
                    <td>
                        <button id="editBtn" type="button" class="btn btn-info"><i class="fas fa-edit mr-1"></i>Edit</button>
                        <button id="deleteBtn" type="button" class="btn btn-danger"><i class="fa fa-trash mr-1" aria-hidden="true"></i>Delete</button>
                    </td>
                </tr>
            }

        </tbody>
    </table>
}

else
{
    <div class="alert alert-danger" role="alert">
        Sorry! No records exists.
    </div>
}